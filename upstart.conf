#!upstart

description "chart"
author      "wavded"

start on (local-filesystems and net-device-up IFACE=eth0)
stop on shutdown

respawn
env NODE_ENV=production
console log

exec /usr/local/n/versions/0.8.1/bin/node /var/www/chart/app.js
